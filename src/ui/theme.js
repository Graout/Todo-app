const Dark = {
    Theme: {
        mainColor: "#fff",
        mainBackground: "#1114",
        mainBackgroundframbar: "#111",
        mainTextframbarcolor: "#fff",
        onHover: "#fff4",
        boxBackground: "#1114"
    }
};

const defaultLayout = {
    Layout: {
        defaultDisplay: "flex",
        defaultHeight: "30px",
        defaultListStyle: "none",
        defaultTextDecoration: "none",
        defaultCursor: "pointer",
        defaultFontSize: "10pt"
    }
};

export { defaultLayout, Dark };
